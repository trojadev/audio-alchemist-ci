Audio Alchemist CLI

A Python-based utility designed to automate the normalization of .wav audio files using the SoX (Sound eXchange) library. This tool processes audio files to a peak level of -3dB and generates a JSON-formatted summary of the processed files, including duration and channel configuration.